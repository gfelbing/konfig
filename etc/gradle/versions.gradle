def groupedVersions = [
        'kotlin': '1.6.10'
]

ext.versions = [
        'org.jetbrains.kotlin:kotlin-stdlib-jdk8': groupedVersions['kotlin'],
        'org.testng:testng'                      : '7.5',
        'org.hamcrest:hamcrest-all'              : '1.3',

]

ext.versions.dependency = { name ->
    return "$name:${ext.versions[name]}"
}
